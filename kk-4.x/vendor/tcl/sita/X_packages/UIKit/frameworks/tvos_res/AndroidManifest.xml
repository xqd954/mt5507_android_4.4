<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tvos"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />
        
        
       <!-- ====================================== -->
    <!-- Permissions for things that cost money -->
    <!-- ====================================== -->
    <eat-comment />

    <!-- Used for permissions that can be used to make the user spend money
         without their direct involvement. -->
    <permission-group android:name="android.permission-group.COST_MONEY"
        android:label="@*android:string/permgrouplab_costMoney"
        android:description="@*android:string/permgroupdesc_costMoney" />

    <!-- ================================== -->
    <!-- Permissions for accessing messages -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Used for permissions that allow an application to send messages
         on behalf of the user or intercept messages being received by the
         user.  This is primarily intended for SMS/MMS messaging, such as
         receiving or reading an MMS. -->
    <permission-group android:name="android.permission-group.MESSAGES"
        android:label="@*android:string/permgrouplab_messages"
        android:icon="@*android:drawable/perm_group_messages"
        android:description="@*android:string/permgroupdesc_messages"
        android:permissionGroupFlags="personalInfo"
        android:priority="360"/>

    <!-- Allows an application to send SMS messages. -->
    <permission android:name="android.permission.SEND_SMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:permissionFlags="costsMoney"
        android:label="@*android:string/permlab_sendSms"
        android:description="@*android:string/permdesc_sendSms" />

    <!-- Allows an application (Phone) to send a request to other applications
         to handle the respond-via-message action during incoming calls.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SEND_RESPOND_VIA_MESSAGE"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_sendRespondViaMessageRequest"
        android:description="@*android:string/permdesc_sendRespondViaMessageRequest" />

    <!-- Allows an application to monitor incoming SMS messages, to record
         or perform processing on them. -->
    <permission android:name="android.permission.RECEIVE_SMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_receiveSms"
        android:description="@*android:string/permdesc_receiveSms" />

    <!-- Allows an application to monitor incoming MMS messages, to record
         or perform processing on them. -->
    <permission android:name="android.permission.RECEIVE_MMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_receiveMms"
        android:description="@*android:string/permdesc_receiveMms" />

    <!-- Allows an application to receive emergency cell broadcast messages,
         to record or display them to the user.
         <p>Not for use by third-party applications.
         @hide Pending API council approval -->
    <permission android:name="android.permission.RECEIVE_EMERGENCY_BROADCAST"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_receiveEmergencyBroadcast"
        android:description="@*android:string/permdesc_receiveEmergencyBroadcast" />

    <!-- Allows an application to read previously received cell broadcast
         messages and to register a content observer to get notifications when
         a cell broadcast has been received and added to the database. For
         emergency alerts, the database is updated immediately after the
         alert dialog and notification sound/vibration/speech are presented.
         The "read" column is then updated after the user dismisses the alert.
         This enables supplementary emergency assistance apps to start loading
         additional emergency information (if Internet access is available)
         when the alert is first received, and to delay presenting the info
         to the user until after the initial alert dialog is dismissed.
         @hide Pending API council approval -->
    <permission android:name="android.permission.READ_CELL_BROADCASTS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readCellBroadcasts"
        android:description="@*android:string/permdesc_readCellBroadcasts" />

    <!-- Allows an application to read SMS messages. -->
    <permission android:name="android.permission.READ_SMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readSms"
        android:description="@*android:string/permdesc_readSms" />

    <!-- Allows an application to write SMS messages. -->
    <permission android:name="android.permission.WRITE_SMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeSms"
        android:description="@*android:string/permdesc_writeSms" />

    <!-- Allows an application to monitor incoming WAP push messages. -->
    <permission android:name="android.permission.RECEIVE_WAP_PUSH"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_receiveWapPush"
        android:description="@*android:string/permdesc_receiveWapPush" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing social info (contacts and social) -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the user's social connections,
         such as contacts, call logs, social stream, etc.  This includes
         both reading and writing of this data (which should generally be
         expressed as two distinct permissions). -->

    <permission-group android:name="android.permission-group.SOCIAL_INFO"
        android:label="@*android:string/permgrouplab_socialInfo"
        android:icon="@*android:drawable/perm_group_social_info"
        android:description="@*android:string/permgroupdesc_socialInfo"
        android:permissionGroupFlags="personalInfo"
        android:priority="320" />

    <!-- Allows an application to read the user's contacts data. -->
    <permission android:name="android.permission.READ_CONTACTS"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readContacts"
        android:description="@*android:string/permdesc_readContacts" />

    <!-- Allows an application to write (but not read) the user's
         contacts data. -->
    <permission android:name="android.permission.WRITE_CONTACTS"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeContacts"
        android:description="@*android:string/permdesc_writeContacts" />

    <!-- Allows an application to execute contacts directory search.
         This should only be used by ContactsProvider.
         <p>Not for use by third-party applications. -->
    <!-- @hide -->
    <permission android:name="android.permission.BIND_DIRECTORY_SEARCH"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to read the user's call log.
         <p class="note"><strong>Note:</strong> If your app uses the
         {@link #READ_CONTACTS} permission and <em>both</em> your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#min">{@code
         minSdkVersion}</a> and <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> values are set to 15 or lower, the system implicitly
         grants your app this permission. If you don't need this permission, be sure your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> is 16 or higher.</p> -->
    <permission android:name="android.permission.READ_CALL_LOG"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readCallLog"
        android:description="@*android:string/permdesc_readCallLog" />

    <!-- Allows an application to write (but not read) the user's
         contacts data.
         <p class="note"><strong>Note:</strong> If your app uses the
         {@link #WRITE_CONTACTS} permission and <em>both</em> your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#min">{@code
         minSdkVersion}</a> and <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> values are set to 15 or lower, the system implicitly
         grants your app this permission. If you don't need this permission, be sure your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> is 16 or higher.</p>  -->
    <permission android:name="android.permission.WRITE_CALL_LOG"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeCallLog"
        android:description="@*android:string/permdesc_writeCallLog" />

  <!-- Allows an application to read from the user's social stream. -->
    <permission android:name="android.permission.READ_SOCIAL_STREAM"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readSocialStream"
        android:description="@*android:string/permdesc_readSocialStream" />

    <!-- Allows an application to write (but not read) the user's
         social stream data. -->
    <permission android:name="android.permission.WRITE_SOCIAL_STREAM"
        android:permissionGroup="android.permission-group.SOCIAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeSocialStream"
        android:description="@*android:string/permdesc_writeSocialStream" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing information about the device owner    -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to information about the device
         user such as profile information.  This includes both reading and
         writing of this data (which should generally be expressed as two
         distinct permissions). -->
    <permission-group android:name="android.permission-group.PERSONAL_INFO"
        android:label="@*android:string/permgrouplab_personalInfo"
        android:icon="@*android:drawable/perm_group_personal_info"
        android:description="@*android:string/permgroupdesc_personalInfo"
        android:permissionGroupFlags="personalInfo"
        android:priority="310" />

    <!-- Allows an application to read the user's personal profile data. -->
    <permission android:name="android.permission.READ_PROFILE"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readProfile"
        android:description="@*android:string/permdesc_readProfile" />

    <!-- Allows an application to write (but not read) the user's
         personal profile data. -->
    <permission android:name="android.permission.WRITE_PROFILE"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeProfile"
        android:description="@*android:string/permdesc_writeProfile" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing the device calendar                   -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the device
         calendar to create / view events.-->
    <permission-group android:name="android.permission-group.CALENDAR"
        android:label="@*android:string/permgrouplab_calendar"
        android:icon="@*android:drawable/perm_group_calendar"
        android:description="@*android:string/permgroupdesc_calendar"
        android:permissionGroupFlags="personalInfo"
        android:priority="290" />

    <!-- Allows an application to read the user's calendar data. -->
    <permission android:name="android.permission.READ_CALENDAR"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readCalendar"
        android:description="@*android:string/permdesc_readCalendar" />

    <!-- Allows an application to write (but not read) the user's
         calendar data. -->
    <permission android:name="android.permission.WRITE_CALENDAR"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_writeCalendar"
        android:description="@*android:string/permdesc_writeCalendar" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing the user dictionary-->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the user
         calendar to create / view events.-->
    <permission-group android:name="android.permission-group.USER_DICTIONARY"
        android:label="@*android:string/permgrouplab_dictionary"
        android:icon="@*android:drawable/perm_group_user_dictionary"
        android:description="@*android:string/permgroupdesc_dictionary"
        android:permissionGroupFlags="personalInfo"
        android:priority="170" />

    <!-- Allows an application to read the user dictionary. This should
         really only be required by an IME, or a dictionary editor like
         the Settings app. -->
    <permission android:name="android.permission.READ_USER_DICTIONARY"
        android:permissionGroup="android.permission-group.USER_DICTIONARY"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readDictionary"
        android:description="@*android:string/permdesc_readDictionary" />

    <!-- Used for permissions that provide access to the user
         calendar to create / view events.-->
    <permission-group android:name="android.permission-group.WRITE_USER_DICTIONARY"
        android:label="@*android:string/permgrouplab_writeDictionary"
        android:icon="@*android:drawable/perm_group_user_dictionary_write"
        android:description="@*android:string/permgroupdesc_writeDictionary"
        android:priority="160" />

    <!-- Allows an application to write to the user dictionary. -->
    <permission android:name="android.permission.WRITE_USER_DICTIONARY"
        android:permissionGroup="android.permission-group.WRITE_USER_DICTIONARY"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_writeDictionary"
        android:description="@*android:string/permdesc_writeDictionary" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing the user bookmarks                    -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the user
         bookmarks and browser history.-->
    <permission-group android:name="android.permission-group.BOOKMARKS"
        android:label="@*android:string/permgrouplab_bookmarks"
        android:icon="@*android:drawable/perm_group_bookmarks"
        android:description="@*android:string/permgroupdesc_bookmarks"
        android:permissionGroupFlags="personalInfo"
        android:priority="300" />

    <!-- Allows an application to read (but not write) the user's
        browsing history and bookmarks. -->
    <permission android:name="com.android.browser.permission.READ_HISTORY_BOOKMARKS"
        android:permissionGroup="android.permission-group.BOOKMARKS"
        android:label="@*android:string/permlab_readHistoryBookmarks"
        android:description="@*android:string/permdesc_readHistoryBookmarks"
        android:protectionLevel="dangerous" />

    <!-- Allows an application to write (but not read) the user's
        browsing history and bookmarks. -->
    <permission android:name="com.android.browser.permission.WRITE_HISTORY_BOOKMARKS"
        android:permissionGroup="android.permission-group.BOOKMARKS"
        android:label="@*android:string/permlab_writeHistoryBookmarks"
        android:description="@*android:string/permdesc_writeHistoryBookmarks"
        android:protectionLevel="dangerous" />

    <!-- =============================================================== -->
    <!-- Permissions for setting the device alarm                        -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the user voicemail box. -->
    <permission-group android:name="android.permission-group.DEVICE_ALARMS"
        android:label="@*android:string/permgrouplab_deviceAlarms"
        android:icon="@*android:drawable/perm_group_device_alarms"
        android:description="@*android:string/permgroupdesc_deviceAlarms"
        android:permissionGroupFlags="personalInfo"
        android:priority="210" />

    <!-- Allows an application to broadcast an Intent to set an alarm for the
         user. -->
    <permission android:name="com.android.alarm.permission.SET_ALARM"
        android:permissionGroup="android.permission-group.DEVICE_ALARMS"
        android:label="@*android:string/permlab_setAlarm"
        android:description="@*android:string/permdesc_setAlarm"
        android:protectionLevel="normal" />

    <!-- =============================================================== -->
    <!-- Permissions for accessing the user voicemail                    -->
    <!-- =============================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide access to the user voicemail box. -->
    <permission-group android:name="android.permission-group.VOICEMAIL"
        android:label="@*android:string/permgrouplab_voicemail"
        android:icon="@*android:drawable/perm_group_voicemail"
        android:description="@*android:string/permgroupdesc_voicemail"
        android:permissionGroupFlags="personalInfo"
        android:priority="280" />

   <!-- Allows an application to add voicemails into the system. -->
    <permission android:name="com.android.voicemail.permission.ADD_VOICEMAIL"
        android:permissionGroup="android.permission-group.VOICEMAIL"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_addVoicemail"
        android:description="@*android:string/permdesc_addVoicemail" />

    <!-- =============================================== -->
    <!-- Permissions for enabling accessibility features -->
    <!-- =============================================== -->
    <eat-comment />

    <!-- Used for permissions that allow requesting certain accessibility features. -->
    <permission-group android:name="android.permission-group.ACCESSIBILITY_FEATURES"
        android:label="@*android:string/permgrouplab_accessibilityFeatures"
        android:icon="@*android:drawable/perm_group_accessibility_features"
        android:description="@*android:string/permgroupdesc_accessibilityFeatures"
        android:priority="380" />

    <!-- ======================================= -->
    <!-- Permissions for accessing location info -->
    <!-- ======================================= -->
    <eat-comment />

    <!-- Used for permissions that allow access to the user's current
         location. -->
    <permission-group android:name="android.permission-group.LOCATION"
        android:label="@*android:string/permgrouplab_location"
        android:icon="@*android:drawable/perm_group_location"
        android:description="@*android:string/permgroupdesc_location"
        android:permissionGroupFlags="personalInfo"
        android:priority="330" />

    <!-- Allows an app to access precise location from location sources such
         as GPS, cell towers, and Wi-Fi. -->
    <permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:permissionGroup="android.permission-group.LOCATION"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_accessFineLocation"
        android:description="@*android:string/permdesc_accessFineLocation" />

    <!-- Allows an app to access approximate location derived from network location
         sources such as cell towers and Wi-Fi. -->
    <permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:permissionGroup="android.permission-group.LOCATION"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_accessCoarseLocation"
        android:description="@*android:string/permdesc_accessCoarseLocation" />

    <!-- Allows an application to create mock location providers for testing -->
    <permission android:name="android.permission.ACCESS_MOCK_LOCATION"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_accessMockLocation"
        android:description="@*android:string/permdesc_accessMockLocation" />

    <!-- Allows an application to access extra location provider commands -->
    <permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_accessLocationExtraCommands"
        android:description="@*android:string/permdesc_accessLocationExtraCommands" />

    <!-- Allows an application to install a location provider into the Location Manager.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.INSTALL_LOCATION_PROVIDER"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_installLocationProvider"
        android:description="@*android:string/permdesc_installLocationProvider" />

    <!-- Allows an application to use location features in hardware,
         such as the geofencing api.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.LOCATION_HARDWARE"
        android:permissionGroup="android.permission-group.LOCATION"
        android:protectionLevel="signature|system" />
    <uses-permission android:name="android.permission.LOCATION_HARDWARE"/>

    <!-- ======================================= -->
    <!-- Permissions for accessing networks -->
    <!-- ======================================= -->
    <eat-comment />

    <!-- Used for permissions that provide access to networking services.  The
         main permission here is internet access, but this is also an
         appropriate group for accessing or modifying any network configuration
         or other related network operations. -->
    <permission-group android:name="android.permission-group.NETWORK"
        android:label="@*android:string/permgrouplab_network"
        android:icon="@*android:drawable/perm_group_network"
        android:description="@*android:string/permgroupdesc_network"
        android:priority="270" />

    <!-- Allows applications to open network sockets. -->
    <permission android:name="android.permission.INTERNET"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_createNetworkSockets"
        android:label="@*android:string/permlab_createNetworkSockets" />

    <!-- Allows applications to access information about networks -->
    <permission android:name="android.permission.ACCESS_NETWORK_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_accessNetworkState"
        android:label="@*android:string/permlab_accessNetworkState" />

    <!-- Allows applications to access information about Wi-Fi networks -->
    <permission android:name="android.permission.ACCESS_WIFI_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_accessWifiState"
        android:label="@*android:string/permlab_accessWifiState" />

    <!-- Allows applications to change Wi-Fi connectivity state -->
    <permission android:name="android.permission.CHANGE_WIFI_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_changeWifiState"
        android:label="@*android:string/permlab_changeWifiState" />

    <!-- @hide -->
    <permission android:name="android.permission.ACCESS_WIMAX_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_accessWimaxState"
        android:label="@*android:string/permlab_accessWimaxState" />

    <!-- @hide -->
    <permission android:name="android.permission.CHANGE_WIMAX_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_changeWimaxState"
        android:label="@*android:string/permlab_changeWimaxState" />

    <!-- ======================================= -->
    <!-- Permissions for short range, peripheral networks -->
    <!-- ======================================= -->
    <eat-comment />

    <!-- Used for permissions that provide access to other devices through Bluetooth.-->
    <permission-group android:name="android.permission-group.BLUETOOTH_NETWORK"
        android:label="@*android:string/permgrouplab_bluetoothNetwork"
        android:icon="@*android:drawable/perm_group_bluetooth"
        android:description="@*android:string/permgroupdesc_bluetoothNetwork"
        android:priority="260" />

    <!-- Allows applications to connect to paired bluetooth devices -->
    <permission android:name="android.permission.BLUETOOTH"
        android:permissionGroup="android.permission-group.BLUETOOTH_NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_bluetooth"
        android:label="@*android:string/permlab_bluetooth" />

    <!-- Allows applications to discover and pair bluetooth devices -->
    <permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:permissionGroup="android.permission-group.BLUETOOTH_NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_bluetoothAdmin"
        android:label="@*android:string/permlab_bluetoothAdmin" />

    <!-- Allows applications to pair bluetooth devices without user interaction.
         This is not available to third party applications. -->
    <permission android:name="android.permission.BLUETOOTH_PRIVILEGED"
        android:permissionGroup="android.permission-group.BLUETOOTH_NETWORK"
        android:protectionLevel="system|signature"
        android:description="@*android:string/permdesc_bluetoothPriv"
        android:label="@*android:string/permlab_bluetoothPriv" />

    <!-- Allows bluetooth stack to access files
         @hide This should only be used by Bluetooth apk.
    -->
    <permission android:name="android.permission.BLUETOOTH_STACK"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Allows applications to perform I/O operations over NFC -->
    <permission android:name="android.permission.NFC"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_nfc"
        android:label="@*android:string/permlab_nfc" />

    <!-- Allows an internal user to use privileged ConnectivityManager APIs.
        @hide -->
    <permission android:name="android.permission.CONNECTIVITY_INTERNAL"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="signature|system" />

    <!-- @hide -->
    <permission android:name="android.permission.RECEIVE_DATA_ACTIVITY_CHANGE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="signature|system" />

    <!-- Allows access to the loop radio (Android@Home mesh network) device.
	@hide -->
    <permission android:name="android.permission.LOOP_RADIO"
	android:permissionGroup="android.permission-group.NETWORK"
	android:protectionLevel="signature|system" />

    <!-- ================================== -->
    <!-- Permissions for accessing accounts -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Permissions for direct access to the accounts managed
         by the Account Manager. -->
    <permission-group android:name="android.permission-group.ACCOUNTS"
        android:label="@*android:string/permgrouplab_accounts"
        android:icon="@*android:drawable/perm_group_accounts"
        android:description="@*android:string/permgroupdesc_accounts"
        android:permissionGroupFlags="personalInfo"
        android:priority="200" />

    <!-- Allows access to the list of accounts in the Accounts Service -->
    <permission android:name="android.permission.GET_ACCOUNTS"
        android:permissionGroup="android.permission-group.ACCOUNTS"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_getAccounts"
        android:label="@*android:string/permlab_getAccounts" />

    <!-- Allows an application to act as an AccountAuthenticator for
         the AccountManager -->
    <permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"
        android:permissionGroup="android.permission-group.ACCOUNTS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_authenticateAccounts"
        android:description="@*android:string/permdesc_authenticateAccounts" />

    <!-- Allows an application to request authtokens from the AccountManager -->
    <permission android:name="android.permission.USE_CREDENTIALS"
        android:permissionGroup="android.permission-group.ACCOUNTS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_useCredentials"
        android:description="@*android:string/permdesc_useCredentials" />

    <!-- Allows an application to manage the list of accounts in the AccountManager -->
    <permission android:name="android.permission.MANAGE_ACCOUNTS"
        android:permissionGroup="android.permission-group.ACCOUNTS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_manageAccounts"
        android:description="@*android:string/permdesc_manageAccounts" />

    <!-- Allows applications to call into AccountAuthenticators.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.ACCOUNT_MANAGER"
        android:permissionGroup="android.permission-group.ACCOUNTS"
        android:protectionLevel="signature"
        android:description="@*android:string/permdesc_accountManagerService"
        android:label="@*android:string/permlab_accountManagerService" />

    <!-- ================================== -->
    <!-- Permissions for accessing hardware that may effect battery life-->
    <!-- ================================== -->
    <eat-comment />

    <!-- Used for permissions that provide direct access to the hardware on
         the device that has an effect on battery life.  This includes vibrator,
         flashlight,  etc. -->

    <permission-group android:name="android.permission-group.AFFECTS_BATTERY"
        android:label="@*android:string/permgrouplab_affectsBattery"
        android:icon="@*android:drawable/perm_group_affects_battery"
        android:description="@*android:string/permgroupdesc_affectsBattery"
        android:priority="180" />

    <!-- Allows applications to enter Wi-Fi Multicast mode -->
    <permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"
        android:permissionGroup="android.permission-group.AFFECTS_BATTERY"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_changeWifiMulticastState"
        android:label="@*android:string/permlab_changeWifiMulticastState" />

    <!-- Allows access to the vibrator -->
    <permission android:name="android.permission.VIBRATE"
        android:permissionGroup="android.permission-group.AFFECTS_BATTERY"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_vibrate"
        android:description="@*android:string/permdesc_vibrate" />

    <!-- Allows access to the flashlight -->
    <permission android:name="android.permission.FLASHLIGHT"
        android:permissionGroup="android.permission-group.AFFECTS_BATTERY"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_flashlight"
        android:description="@*android:string/permdesc_flashlight" />

    <!-- Allows using PowerManager WakeLocks to keep processor from sleeping or screen
         from dimming -->
    <permission android:name="android.permission.WAKE_LOCK"
        android:permissionGroup="android.permission-group.AFFECTS_BATTERY"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_wakeLock"
        android:description="@*android:string/permdesc_wakeLock" />

    <!-- Allows using the device's IR transmitter, if available -->
    <permission android:name="android.permission.TRANSMIT_IR"
        android:permissionGroup="android.permission-group.AFFECTS_BATTERY"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_transmitIr"
        android:description="@*android:string/permdesc_transmitIr" />

    <!-- ==================================================== -->
    <!-- Permissions related to changing audio settings   -->
    <!-- ==================================================== -->
    <eat-comment />

    <!-- Used for permissions that provide direct access to speaker settings
         the device. -->
    <permission-group android:name="android.permission-group.AUDIO_SETTINGS"
        android:label="@*android:string/permgrouplab_audioSettings"
        android:icon="@*android:drawable/perm_group_audio_settings"
        android:description="@*android:string/permgroupdesc_audioSettings"
        android:priority="130" />

    <!-- Allows an application to modify global audio settings -->
    <permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"
        android:permissionGroup="android.permission-group.AUDIO_SETTINGS"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_modifyAudioSettings"
        android:description="@*android:string/permdesc_modifyAudioSettings" />

    <!-- ================================== -->
    <!-- Permissions for accessing hardware -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Used for permissions that provide direct access to the hardware on
         the device.  This includes audio, the camera, vibrator, etc. -->
    <permission-group android:name="android.permission-group.HARDWARE_CONTROLS"
        android:label="@*android:string/permgrouplab_hardwareControls"
        android:description="@*android:string/permgroupdesc_hardwareControls"
        android:priority="260"/>

    <!-- Allows an application to manage preferences and permissions for USB devices
         @hide -->
    <permission android:name="android.permission.MANAGE_USB"
        android:permissionGroup="android.permission-group.HARDWARE_CONTROLS"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_manageUsb"
        android:description="@*android:string/permdesc_manageUsb" />

    <!-- Allows an application to access the MTP USB kernel driver.
         For use only by the device side MTP implementation.
         @hide -->
    <permission android:name="android.permission.ACCESS_MTP"
        android:permissionGroup="android.permission-group.HARDWARE_CONTROLS"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_accessMtp"
        android:description="@*android:string/permdesc_accessMtp" />

    <!-- Allows access to hardware peripherals.  Intended only for hardware testing.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.HARDWARE_TEST"
        android:permissionGroup="android.permission-group.HARDWARE_CONTROLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_hardware_test"
        android:description="@*android:string/permdesc_hardware_test" />

    <!-- Allows access to configure network interfaces, configure/use IPSec, etc.
         @hide -->
    <permission android:name="android.permission.NET_ADMIN"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Allows registration for remote audio playback. @hide -->
    <permission android:name="android.permission.REMOTE_AUDIO_PLAYBACK"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- =========================================== -->
    <!-- Permissions associated with audio capture -->
    <!-- =========================================== -->
    <eat-comment />

    <!-- Used for permissions that are associated with accessing
         microphone audio from the device. Note that phone calls also capture audio
         but are in a separate (more visible) permission group. -->
    <permission-group android:name="android.permission-group.MICROPHONE"
        android:label="@*android:string/permgrouplab_microphone"
        android:icon="@*android:drawable/perm_group_microphone"
        android:description="@*android:string/permgroupdesc_microphone"
        android:permissionGroupFlags="personalInfo"
        android:priority="340" />

    <!-- Allows an application to record audio -->
    <permission android:name="android.permission.RECORD_AUDIO"
        android:permissionGroup="android.permission-group.MICROPHONE"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_recordAudio"
        android:description="@*android:string/permdesc_recordAudio" />


    <!-- =========================================== -->
    <!-- Permissions associated with camera and image capture -->
    <!-- =========================================== -->
    <eat-comment />

    <!-- Used for permissions that are associated with accessing
         camera or capturing images/video from the device. -->
    <permission-group android:name="android.permission-group.CAMERA"
        android:label="@*android:string/permgrouplab_camera"
        android:icon="@*android:drawable/perm_group_camera"
        android:description="@*android:string/permgroupdesc_camera"
        android:permissionGroupFlags="personalInfo"
        android:priority="350" />

    <!-- Required to be able to access the camera device.
         <p>This will automatically enforce the <a
         href="{@docRoot}guide/topics/manifest/uses-feature-element.html">{@code
         &lt;uses-feature&gt;}</a> manifest element for <em>all</em> camera features.
         If you do not require all camera features or can properly operate if a camera
         is not available, then you must modify your manifest as appropriate in order to
         install on devices that don't support all camera features.</p> -->
    <permission android:name="android.permission.CAMERA"
        android:permissionGroup="android.permission-group.CAMERA"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_camera"
        android:description="@*android:string/permdesc_camera" />

    <!-- Allows disabling the transmit-indicator LED that is normally on when
         a camera is in use by an application.
         @hide -->
    <permission android:name="android.permission.CAMERA_DISABLE_TRANSMIT_LED"
        android:permissionGroup="android.permission-group.CAMERA"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_cameraDisableTransmitLed"
        android:description="@*android:string/permdesc_cameraDisableTransmitLed" />

    <!-- =========================================== -->
    <!-- Permissions associated with telephony state -->
    <!-- =========================================== -->
    <eat-comment />

    <!-- Used for permissions that are associated with accessing and modifyign
         telephony state: placing calls, intercepting outgoing calls, reading
         and modifying the phone state. -->
    <permission-group android:name="android.permission-group.PHONE_CALLS"
        android:label="@*android:string/permgrouplab_phoneCalls"
        android:icon="@*android:drawable/perm_group_phone_calls"
        android:description="@*android:string/permgroupdesc_phoneCalls"
        android:permissionGroupFlags="personalInfo"
        android:priority="370" />

    <!-- Allows an application to see the number being dialed during an outgoing
         call with the option to redirect the call to a different number or
         abort the call altogether. -->
    <permission android:name="android.permission.PROCESS_OUTGOING_CALLS"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_processOutgoingCalls"
        android:description="@*android:string/permdesc_processOutgoingCalls" />

    <!-- Allows modification of the telephony state - power on, mmi, etc.
         Does not include placing calls.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.MODIFY_PHONE_STATE"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_modifyPhoneState"
        android:description="@*android:string/permdesc_modifyPhoneState" />

    <!-- Allows read only access to phone state.
         <p class="note"><strong>Note:</strong> If <em>both</em> your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#min">{@code
         minSdkVersion}</a> and <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> values are set to 3 or lower, the system implicitly
         grants your app this permission. If you don't need this permission, be sure your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> is 4 or higher. -->
    <permission android:name="android.permission.READ_PHONE_STATE"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_readPhoneState"
        android:description="@*android:string/permdesc_readPhoneState" />

    <!-- Allows read access to privileged phone state.
         @hide Used internally. -->
    <permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="signature|system" />

     <!-- Allows an application to initiate a phone call without going through
         the Dialer user interface for the user to confirm the call
         being placed. -->
    <permission android:name="android.permission.CALL_PHONE"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="dangerous"
        android:permissionFlags="costsMoney"
        android:label="@*android:string/permlab_callPhone"
        android:description="@*android:string/permdesc_callPhone" />

    <!-- Allows an application to use SIP service -->
    <permission android:name="android.permission.USE_SIP"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_use_sip"
        android:label="@*android:string/permlab_use_sip" />

    <!-- Allows an application to request CallHandlerService implementations.
         @hide -->
    <permission android:name="android.permission.BIND_CALL_SERVICE"
        android:permissionGroup="android.permission-group.PHONE_CALLS"
        android:protectionLevel="system|signature"
        android:description="@*android:string/permdesc_bind_call_service"
        android:label="@*android:string/permlab_bind_call_service" />

    <!-- ================================== -->
    <!-- Permissions for sdcard interaction -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Group of permissions that are related to SD card access. -->
    <permission-group android:name="android.permission-group.STORAGE"
        android:label="@*android:string/permgrouplab_storage"
        android:icon="@*android:drawable/perm_group_storage"
        android:description="@*android:string/permgroupdesc_storage"
        android:permissionGroupFlags="personalInfo"
        android:priority="240" />

    <!-- Allows an application to read from external storage.
         <p>Any app that declares the {@link #WRITE_EXTERNAL_STORAGE} permission is implicitly
         granted this permission.</p>
         <p>This permission is enforced starting in API level 19.  Before API level 19, this
         permission is not enforced and all apps still have access to read from external storage.
         You can test your app with the permission enforced by enabling <em>Protect USB
         storage</em> under Developer options in the Settings app on a device running Android 4.1 or
         higher.</p>
         <p>Also starting in API level 19, this permission is <em>not</em> required to
         read/write files in your application-specific directories returned by
         {@link android.content.Context#getExternalFilesDir} and
         {@link android.content.Context#getExternalCacheDir}.
         <p class="note"><strong>Note:</strong> If <em>both</em> your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#min">{@code
         minSdkVersion}</a> and <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> values are set to 3 or lower, the system implicitly
         grants your app this permission. If you don't need this permission, be sure your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> is 4 or higher.-->
    <permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:permissionGroup="android.permission-group.STORAGE"
        android:label="@*android:string/permlab_sdcardRead"
        android:description="@*android:string/permdesc_sdcardRead"
        android:protectionLevel="normal" />

    <!-- Allows an application to write to external storage.
         <p class="note"><strong>Note:</strong> If <em>both</em> your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#min">{@code
         minSdkVersion}</a> and <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> values are set to 3 or lower, the system implicitly
         grants your app this permission. If you don't need this permission, be sure your <a
         href="{@docRoot}guide/topics/manifest/uses-sdk-element.html#target">{@code
         targetSdkVersion}</a> is 4 or higher.
         <p>Starting in API level 19, this permission is <em>not</em> required to
         read/write files in your application-specific directories returned by
         {@link android.content.Context#getExternalFilesDir} and
         {@link android.content.Context#getExternalCacheDir}. -->
    <permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:permissionGroup="android.permission-group.STORAGE"
        android:label="@*android:string/permlab_sdcardWrite"
        android:description="@*android:string/permdesc_sdcardWrite"
        android:protectionLevel="dangerous" />

    <!-- Allows an application to write to internal media storage
         @hide  -->
    <permission android:name="android.permission.WRITE_MEDIA_STORAGE"
        android:permissionGroup="android.permission-group.STORAGE"
        android:label="@*android:string/permlab_mediaStorageWrite"
        android:description="@*android:string/permdesc_mediaStorageWrite"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to manage access to documents, usually as part
         of a document picker. -->
    <permission android:name="android.permission.MANAGE_DOCUMENTS"
        android:permissionGroup="android.permission-group.STORAGE"
        android:label="@*android:string/permlab_manageDocs"
        android:description="@*android:string/permdesc_manageDocs"
        android:protectionLevel="signature" />

    <!-- ================================== -->
    <!-- Permissions for screenlock         -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Group of permissions that are related to the screenlock. -->
    <permission-group android:name="android.permission-group.SCREENLOCK"
        android:label="@*android:string/permgrouplab_screenlock"
        android:icon="@*android:drawable/perm_group_screenlock"
        android:permissionGroupFlags="personalInfo"
        android:description="@*android:string/permgroupdesc_screenlock"
        android:priority="230" />

    <!-- Allows applications to disable the keyguard -->
    <permission android:name="android.permission.DISABLE_KEYGUARD"
        android:permissionGroup="android.permission-group.SCREENLOCK"
        android:protectionLevel="dangerous"
        android:description="@*android:string/permdesc_disableKeyguard"
        android:label="@*android:string/permlab_disableKeyguard" />

    <!-- ================================== -->
    <!-- Permissions to access other installed applications  -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Group of permissions that are related to the other applications
         installed on the system.  Examples include such as listing
         running apps, or killing background processes. -->
    <permission-group android:name="android.permission-group.APP_INFO"
        android:label="@*android:string/permgrouplab_appInfo"
        android:icon="@*android:drawable/perm_group_app_info"
        android:description="@*android:string/permgroupdesc_appInfo"
        android:priority="220" />

    <!-- Allows an application to get information about the currently
         or recently running tasks. -->
    <permission android:name="android.permission.GET_TASKS"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_getTasks"
        android:description="@*android:string/permdesc_getTasks" />
    <!-- @hide Allows an application to call APIs that allow it to do interactions
         across the users on the device, using singleton services and
         user-targeted broadcasts.  This permission is not available to
         third party applications. -->
    <permission android:name="android.permission.INTERACT_ACROSS_USERS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_interactAcrossUsers"
        android:description="@*android:string/permdesc_interactAcrossUsers" />

    <!-- @hide Fuller form of {@link android.Manifest.permission#INTERACT_ACROSS_USERS}
         that removes restrictions on where broadcasts can be sent and allows other
         types of interactions. -->
    <permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_interactAcrossUsersFull"
        android:description="@*android:string/permdesc_interactAcrossUsersFull" />

    <!-- @hide Allows an application to call APIs that allow it to query and manage
         users on the device. This permission is not available to
         third party applications. -->
    <permission android:name="android.permission.MANAGE_USERS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_manageUsers"
        android:description="@*android:string/permdesc_manageUsers" />

    <!-- Allows an application to get full detailed information about
         recently running tasks, with full fidelity to the real state.
         @hide -->
    <permission android:name="android.permission.GET_DETAILED_TASKS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_getDetailedTasks"
        android:description="@*android:string/permdesc_getDetailedTasks" />

    <!-- Allows an application to change the Z-order of tasks -->
    <permission android:name="android.permission.REORDER_TASKS"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_reorderTasks"
        android:description="@*android:string/permdesc_reorderTasks" />

    <!-- @hide Allows an application to change to remove/kill tasks -->
    <permission android:name="android.permission.REMOVE_TASKS"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_removeTasks"
        android:description="@*android:string/permdesc_removeTasks" />

    <!-- @hide Allows an application to create/manage/remove stacks -->
    <permission android:name="android.permission.MANAGE_ACTIVITY_STACKS"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_manageActivityStacks"
        android:description="@*android:string/permdesc_manageActivityStacks" />

    <!-- Allows an application to start any activity, regardless of permission
         protection or exported state. @hide -->
    <permission android:name="android.permission.START_ANY_ACTIVITY"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_startAnyActivity"
        android:description="@*android:string/permdesc_startAnyActivity" />

    <!-- @deprecated The {@link android.app.ActivityManager#restartPackage}
        API is no longer supported. -->
    <permission android:name="android.permission.RESTART_PACKAGES"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_killBackgroundProcesses"
        android:description="@*android:string/permdesc_killBackgroundProcesses" />

    <!-- Allows an application to call
        {@link android.app.ActivityManager#killBackgroundProcesses}. -->
    <permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_killBackgroundProcesses"
        android:description="@*android:string/permdesc_killBackgroundProcesses" />

    <!-- ================================== -->
    <!-- Permissions affecting the display of other applications  -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Group of permissions that allow manipulation of how
         another application displays UI to the user. -->
    <permission-group android:name="android.permission-group.DISPLAY"
        android:label="@*android:string/permgrouplab_display"
        android:icon="@*android:drawable/perm_group_display"
        android:description="@*android:string/permgroupdesc_display"
        android:priority="190"/>

    <!-- Allows an application to open windows using the type
         {@link android.view.WindowManager.LayoutParams#TYPE_SYSTEM_ALERT},
         shown on top of all other applications.  Very few applications
         should use this permission; these windows are intended for
         system-level interaction with the user. -->
    <permission android:name="android.permission.SYSTEM_ALERT_WINDOW"
        android:permissionGroup="android.permission-group.DISPLAY"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_systemAlertWindow"
        android:description="@*android:string/permdesc_systemAlertWindow" />

    <!-- ================================== -->
    <!-- Permissions affecting the system wallpaper -->
    <!-- ================================== -->
    <eat-comment />

    <!-- Group of permissions that allow manipulation of how
         another application displays UI to the user. -->
    <permission-group android:name="android.permission-group.WALLPAPER"
        android:label="@*android:string/permgrouplab_wallpaper"
        android:icon="@*android:drawable/perm_group_wallpaper"
        android:description="@*android:string/permgroupdesc_wallpaper"
        android:priority="150" />

    <!-- Allows applications to set the wallpaper -->
    <permission android:name="android.permission.SET_WALLPAPER"
        android:permissionGroup="android.permission-group.WALLPAPER"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_setWallpaper"
        android:description="@*android:string/permdesc_setWallpaper" />

    <!-- Allows applications to set the wallpaper hints -->
    <permission android:name="android.permission.SET_WALLPAPER_HINTS"
        android:permissionGroup="android.permission-group.WALLPAPER"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_setWallpaperHints"
        android:description="@*android:string/permdesc_setWallpaperHints" />

    <!-- ============================================ -->
    <!-- Permissions for changing the system clock -->
    <!-- ============================================ -->
    <eat-comment />

    <!-- Group of permissions that are related to system clock. -->
    <permission-group android:name="android.permission-group.SYSTEM_CLOCK"
        android:label="@*android:string/permgrouplab_systemClock"
        android:icon="@*android:drawable/perm_group_system_clock"
        android:description="@*android:string/permgroupdesc_systemClock"
        android:priority="140" />

    <!-- Allows applications to set the system time.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_TIME"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_setTime"
        android:description="@*android:string/permdesc_setTime" />

    <!-- Allows applications to set the system time zone -->
    <permission android:name="android.permission.SET_TIME_ZONE"
        android:permissionGroup="android.permission-group.SYSTEM_CLOCK"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_setTimeZone"
        android:description="@*android:string/permdesc_setTimeZone" />

    <!-- ==================================================== -->
    <!-- Permissions related to changing status bar   -->
    <!-- ==================================================== -->
    <eat-comment />

    <!-- Used for permissions that change the status bar -->
    <permission-group android:name="android.permission-group.STATUS_BAR"
        android:label="@*android:string/permgrouplab_statusBar"
        android:icon="@*android:drawable/perm_group_status_bar"
        android:description="@*android:string/permgroupdesc_statusBar"
        android:priority="110" />

    <!-- Allows an application to expand or collapse the status bar. -->
    <permission android:name="android.permission.EXPAND_STATUS_BAR"
        android:permissionGroup="android.permission-group.STATUS_BAR"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_expandStatusBar"
        android:description="@*android:string/permdesc_expandStatusBar" />

    <!-- ============================================================== -->
    <!-- Permissions related to adding/removing shortcuts from Launcher -->
    <!-- ============================================================== -->
    <eat-comment />

    <!-- Allows an application to install a shortcut in Launcher -->
    <permission
        android:name="com.android.launcher.permission.INSTALL_SHORTCUT"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_install_shortcut"
        android:description="@*android:string/permdesc_install_shortcut" />

        <!-- Allows an application to uninstall a shortcut in Launcher -->
    <permission
        android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_uninstall_shortcut"
        android:description="@*android:string/permdesc_uninstall_shortcut"/>

    <!-- ==================================================== -->
    <!-- Permissions related to accessing sync settings   -->
    <!-- ==================================================== -->
    <eat-comment />

    <!-- Used for permissions that access the sync settings or sync
         related information. -->
    <permission-group android:name="android.permission-group.SYNC_SETTINGS"
        android:label="@*android:string/permgrouplab_syncSettings"
        android:icon="@*android:drawable/perm_group_sync_settings"
        android:description="@*android:string/permgroupdesc_syncSettings"
        android:priority="120" />

    <!-- Allows applications to read the sync settings -->
    <permission android:name="android.permission.READ_SYNC_SETTINGS"
        android:permissionGroup="android.permission-group.SYNC_SETTINGS"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_readSyncSettings"
        android:label="@*android:string/permlab_readSyncSettings" />

    <!-- Allows applications to write the sync settings -->
    <permission android:name="android.permission.WRITE_SYNC_SETTINGS"
        android:permissionGroup="android.permission-group.SYNC_SETTINGS"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_writeSyncSettings"
        android:label="@*android:string/permlab_writeSyncSettings" />

    <!-- Allows applications to read the sync stats -->
    <permission android:name="android.permission.READ_SYNC_STATS"
        android:permissionGroup="android.permission-group.SYNC_SETTINGS"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_readSyncStats"
        android:label="@*android:string/permlab_readSyncStats" />


    <!-- ============================================ -->
    <!-- Permissions for low-level system interaction -->
    <!-- ============================================ -->
    <eat-comment />

    <!-- Group of permissions that are related to system APIs.  Many
         of these are not permissions the user will be expected to understand,
         and such permissions should generally be marked as "normal" protection
         level so they don't get displayed.  This can also, however, be used
         for miscellaneous features that provide access to the operating system,
         such as writing the global system settings. -->
    <permission-group android:name="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permgrouplab_systemTools"
        android:icon="@*android:drawable/perm_group_system_tools"
        android:description="@*android:string/permgroupdesc_systemTools"
        android:priority="100" />

    <!-- @hide Change the screen compatibility mode of applications -->
    <permission android:name="android.permission.SET_SCREEN_COMPATIBILITY"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_setScreenCompatibility"
        android:description="@*android:string/permdesc_setScreenCompatibility" />

    <!-- Allows an application to access all multi-user external storage @hide -->
    <permission android:name="android.permission.ACCESS_ALL_EXTERNAL_STORAGE"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:label="@*android:string/permlab_sdcardAccessAll"
        android:description="@*android:string/permdesc_sdcardAccessAll"
        android:protectionLevel="signature" />

    <!-- Allows an application to modify the current configuration, such
         as locale. -->
    <permission android:name="android.permission.CHANGE_CONFIGURATION"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_changeConfiguration"
        android:description="@*android:string/permdesc_changeConfiguration" />

    <!-- Allows an application to read or write the system settings. -->
    <permission android:name="android.permission.WRITE_SETTINGS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_writeSettings"
        android:description="@*android:string/permdesc_writeSettings" />

    <!-- Allows an application to modify the Google service map.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.WRITE_GSERVICES"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_writeGservices"
        android:description="@*android:string/permdesc_writeGservices" />

    <!-- Allows an application to call
        {@link android.app.ActivityManager#forceStopPackage}.
        @hide -->
    <permission android:name="android.permission.FORCE_STOP_PACKAGES"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_forceStopPackages"
        android:description="@*android:string/permdesc_forceStopPackages" />

    <!-- @hide Allows an application to retrieve the content of the active window
         An active window is the window that has fired an accessibility event. -->
    <permission android:name="android.permission.RETRIEVE_WINDOW_CONTENT"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_retrieve_window_content"
        android:description="@*android:string/permdesc_retrieve_window_content" />

    <!-- Modify the global animation scaling factor.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_ANIMATION_SCALE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_setAnimationScale"
        android:description="@*android:string/permdesc_setAnimationScale" />

    <!-- @deprecated This functionality will be removed in the future; please do
         not use. Allow an application to make its activities persistent. -->
    <permission android:name="android.permission.PERSISTENT_ACTIVITY"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_persistentActivity"
        android:description="@*android:string/permdesc_persistentActivity" />

    <!-- Allows an application to find out the space used by any package. -->
    <permission android:name="android.permission.GET_PACKAGE_SIZE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_getPackageSize"
        android:description="@*android:string/permdesc_getPackageSize" />

    <!-- @deprecated No longer useful, see
         {@link android.content.pm.PackageManager#addPackageToPreferred}
         for details. -->
    <permission android:name="android.permission.SET_PREFERRED_APPLICATIONS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_setPreferredApplications"
        android:description="@*android:string/permdesc_setPreferredApplications" />

    <!-- Allows an application to receive the
         {@link android.content.Intent#ACTION_BOOT_COMPLETED} that is
         broadcast after the system finishes booting.  If you don't
         request this permission, you will not receive the broadcast at
         that time.  Though holding this permission does not have any
         security implications, it can have a negative impact on the
         user experience by increasing the amount of time it takes the
         system to start and allowing applications to have themselves
         running without the user being aware of them.  As such, you must
         explicitly declare your use of this facility to make that visible
         to the user. -->
    <permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        android:permissionGroup="android.permission-group.APP_INFO"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_receiveBootCompleted"
        android:description="@*android:string/permdesc_receiveBootCompleted" />

    <!-- Allows an application to broadcast sticky intents.  These are
         broadcasts whose data is held by the system after being finished,
         so that clients can quickly retrieve that data without having
         to wait for the next broadcast. -->
    <permission android:name="android.permission.BROADCAST_STICKY"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="normal"
        android:label="@*android:string/permlab_broadcastSticky"
        android:description="@*android:string/permdesc_broadcastSticky" />

    <!-- Allows mounting and unmounting file systems for removable storage.
    <p>Not for use by third-party applications.-->
    <permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="system|signature"
        android:label="@*android:string/permlab_mount_unmount_filesystems"
        android:description="@*android:string/permdesc_mount_unmount_filesystems" />

    <!-- Allows formatting file systems for removable storage.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.MOUNT_FORMAT_FILESYSTEMS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="system|signature"
        android:label="@*android:string/permlab_mount_format_filesystems"
        android:description="@*android:string/permdesc_mount_format_filesystems" />

    <!-- Allows access to ASEC non-destructive API calls
         @hide  -->
    <permission android:name="android.permission.ASEC_ACCESS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_asec_access"
        android:description="@*android:string/permdesc_asec_access" />

    <!-- Allows creation of ASEC volumes
         @hide  -->
    <permission android:name="android.permission.ASEC_CREATE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_asec_create"
        android:description="@*android:string/permdesc_asec_create" />

    <!-- Allows destruction of ASEC volumes
         @hide  -->
    <permission android:name="android.permission.ASEC_DESTROY"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_asec_destroy"
        android:description="@*android:string/permdesc_asec_destroy" />

    <!-- Allows mount / unmount of ASEC volumes
         @hide  -->
    <permission android:name="android.permission.ASEC_MOUNT_UNMOUNT"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_asec_mount_unmount"
        android:description="@*android:string/permdesc_asec_mount_unmount" />

    <!-- Allows rename of ASEC volumes
         @hide  -->
    <permission android:name="android.permission.ASEC_RENAME"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_asec_rename"
        android:description="@*android:string/permdesc_asec_rename" />

    <!-- Allows applications to write the apn settings.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.WRITE_APN_SETTINGS"
                android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
                android:protectionLevel="signature|system"
                android:description="@*android:string/permdesc_writeApnSettings"
                android:label="@*android:string/permlab_writeApnSettings" />

    <!-- Allows an application to allow access the subscribed feeds
         ContentProvider. -->
    <permission android:name="android.permission.SUBSCRIBED_FEEDS_READ"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permlab_subscribedFeedsRead"
        android:description="@*android:string/permdesc_subscribedFeedsRead"
        android:protectionLevel="normal" />
    <permission android:name="android.permission.SUBSCRIBED_FEEDS_WRITE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permlab_subscribedFeedsWrite"
        android:description="@*android:string/permdesc_subscribedFeedsWrite"
        android:protectionLevel="dangerous" />

    <!-- Allows applications to change network connectivity state -->
    <permission android:name="android.permission.CHANGE_NETWORK_STATE"
        android:permissionGroup="android.permission-group.NETWORK"
        android:protectionLevel="normal"
        android:description="@*android:string/permdesc_changeNetworkState"
        android:label="@*android:string/permlab_changeNetworkState" />

    <!-- Allows an application to clear the caches of all installed
         applications on the device.  -->
    <permission android:name="android.permission.CLEAR_APP_CACHE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="dangerous"
        android:label="@*android:string/permlab_clearAppCache"
        android:description="@*android:string/permdesc_clearAppCache" />

    <!-- Allows an application to use any media decoder when decoding for playback
         @hide -->
    <permission android:name="android.permission.ALLOW_ANY_CODEC_FOR_PLAYBACK"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_anyCodecForPlayback"
        android:description="@*android:string/permdesc_anyCodecForPlayback" />

    <!-- Allows an application to install and/or uninstall CA certificates on
         behalf of the user.
         @hide -->
    <permission android:name="android.permission.MANAGE_CA_CERTIFICATES"
        android:protectionLevel="signature|system"
        android:label="@*android:string/permlab_manageCaCertificates"
        android:description="@*android:string/permdesc_manageCaCertificates" />

    <!-- ========================================= -->
    <!-- Permissions for special development tools -->
    <!-- ========================================= -->
    <eat-comment />

    <!-- Group of permissions that are related to development features.  These
         are not permissions that should appear in third-party applications; they
         protect APIs that are intended only to be used for development
         purposes. -->
    <permission-group android:name="android.permission-group.DEVELOPMENT_TOOLS"
        android:label="@*android:string/permgrouplab_developmentTools"
        android:description="@*android:string/permgroupdesc_developmentTools"
        android:priority="310" />

    <!-- Allows an application to read or write the secure system settings.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_writeSecureSettings"
        android:description="@*android:string/permdesc_writeSecureSettings" />

    <!-- Allows an application to retrieve state dump information from system services.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.DUMP"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_dump"
        android:description="@*android:string/permdesc_dump" />

    <!-- Allows an application to read the low-level system log files.
    <p>Not for use by third-party applications, because
    Log entries can contain the user's private information. -->
    <permission android:name="android.permission.READ_LOGS"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_readLogs"
        android:description="@*android:string/permdesc_readLogs" />

    <!-- Configure an application for debugging.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_DEBUG_APP"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_setDebugApp"
        android:description="@*android:string/permdesc_setDebugApp" />

    <!-- Allows an application to set the maximum number of (not needed)
         application processes that can be running.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_PROCESS_LIMIT"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_setProcessLimit"
        android:description="@*android:string/permdesc_setProcessLimit" />

    <!-- Allows an application to control whether activities are immediately
         finished when put in the background.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_ALWAYS_FINISH"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_setAlwaysFinish"
        android:description="@*android:string/permdesc_setAlwaysFinish" />

    <!-- Allow an application to request that a signal be sent to all persistent processes.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SIGNAL_PERSISTENT_PROCESSES"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS"
        android:protectionLevel="signature|system|development"
        android:label="@*android:string/permlab_signalPersistentProcesses"
        android:description="@*android:string/permdesc_signalPersistentProcesses" />

    <!-- ==================================== -->
    <!-- Private (signature-only) permissions -->
    <!-- ==================================== -->
    <eat-comment />

    <!-- Allows applications to RW to diagnostic resources.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.DIAGNOSTIC"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:description="@*android:string/permdesc_diagnostic"
        android:label="@*android:string/permlab_diagnostic" />

    <!-- Allows an application to open, close, or disable the status bar
         and its icons.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.STATUS_BAR"
        android:label="@*android:string/permlab_statusBar"
        android:description="@*android:string/permdesc_statusBar"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to be the status bar.  Currently used only by SystemUI.apk
    @hide -->
    <permission android:name="android.permission.STATUS_BAR_SERVICE"
        android:label="@*android:string/permlab_statusBarService"
        android:description="@*android:string/permdesc_statusBarService"
        android:protectionLevel="signature" />

    <!-- Allows an application to force a BACK operation on whatever is the
         top activity.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.FORCE_BACK"
        android:label="@*android:string/permlab_forceBack"
        android:description="@*android:string/permdesc_forceBack"
        android:protectionLevel="signature" />

    <!-- Allows an application to update device statistics.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.UPDATE_DEVICE_STATS"
        android:label="@*android:string/permlab_updateBatteryStats"
        android:description="@*android:string/permdesc_updateBatteryStats"
        android:protectionLevel="signature|system" />

    <!-- @hide Allows an application to collect battery statistics -->
    <permission android:name="android.permission.GET_APP_OPS_STATS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permlab_getAppOpsStats"
        android:description="@*android:string/permdesc_getAppOpsStats"
        android:protectionLevel="signature|system|development" />

    <!-- Allows an application to update application operation statistics. Not for
         use by third party apps. @hide -->
    <permission android:name="android.permission.UPDATE_APP_OPS_STATS"
        android:label="@*android:string/permlab_updateAppOpsStats"
        android:description="@*android:string/permdesc_updateAppOpsStats"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to open windows that are for use by parts
         of the system user interface.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        android:label="@*android:string/permlab_internalSystemWindow"
        android:description="@*android:string/permdesc_internalSystemWindow"
        android:protectionLevel="signature" />

    <!-- Allows an application to manage (create, destroy,
         Z-order) application tokens in the window manager.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.MANAGE_APP_TOKENS"
        android:label="@*android:string/permlab_manageAppTokens"
        android:description="@*android:string/permdesc_manageAppTokens"
        android:protectionLevel="signature" />

    <!-- @hide Allows the application to temporarily freeze the screen for a
         full-screen transition. -->
    <permission android:name="android.permission.FREEZE_SCREEN"
        android:label="@*android:string/permlab_freezeScreen"
        android:description="@*android:string/permdesc_freezeScreen"
        android:protectionLevel="signature" />

    <!-- Allows an application to inject user events (keys, touch, trackball)
         into the event stream and deliver them to ANY window.  Without this
         permission, you can only deliver events to windows in your own process.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.INJECT_EVENTS"
        android:label="@*android:string/permlab_injectEvents"
        android:description="@*android:string/permdesc_injectEvents"
        android:protectionLevel="signature" />

    <!-- @hide Allows an application to register an input filter which filters the stream
         of user events (keys, touch, trackball) before they are dispatched to any window. -->
    <permission android:name="android.permission.FILTER_EVENTS"
        android:label="@*android:string/permlab_filter_events"
        android:description="@*android:string/permdesc_filter_events"
        android:protectionLevel="signature" />

    <!-- @hide Allows an application to retrieve info for a window from the window manager. -->
    <permission android:name="android.permission.RETRIEVE_WINDOW_INFO"
        android:label="@*android:string/permlab_retrieve_window_info"
        android:description="@*android:string/permdesc_retrieve_window_info"
        android:protectionLevel="signature" />

    <!-- @hide Allows an application to temporary enable accessibility on the device. -->
    <permission android:name="android.permission.TEMPORARY_ENABLE_ACCESSIBILITY"
        android:label="@*android:string/permlab_temporary_enable_accessibility"
        android:description="@*android:string/permdesc_temporary_enable_accessibility"
        android:protectionLevel="signature" />

    <!-- @hide Allows an application to magnify the content of a display. -->
    <permission android:name="android.permission.MAGNIFY_DISPLAY"
        android:label="@*android:string/permlab_magnify_display"
        android:description="@*android:string/permdesc_magnify_display"
        android:protectionLevel="signature" />

    <!-- Allows an application to watch and control how activities are
         started globally in the system.  Only for is in debugging
         (usually the monkey command).
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_ACTIVITY_WATCHER"
        android:label="@*android:string/permlab_runSetActivityWatcher"
        android:description="@*android:string/permdesc_runSetActivityWatcher"
        android:protectionLevel="signature" />

    <!-- Allows an application to call the activity manager shutdown() API
         to put the higher-level system there into a shutdown state.
         @hide -->
    <permission android:name="android.permission.SHUTDOWN"
        android:label="@*android:string/permlab_shutdown"
        android:description="@*android:string/permdesc_shutdown"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to tell the activity manager to temporarily
         stop application switches, putting it into a special mode that
         prevents applications from immediately switching away from some
         critical UI such as the home screen.
         @hide -->
    <permission android:name="android.permission.STOP_APP_SWITCHES"
        android:label="@*android:string/permlab_stopAppSwitches"
        android:description="@*android:string/permdesc_stopAppSwitches"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to retrieve private information about
         the current top activity, such as any assist context it can provide.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.GET_TOP_ACTIVITY_INFO"
        android:label="@*android:string/permlab_getTopActivityInfo"
        android:description="@*android:string/permdesc_getTopActivityInfo"
        android:protectionLevel="signature" />

    <!-- Allows an application to retrieve the current state of keys and
         switches.
         <p>Not for use by third-party applications.
         @deprecated The API that used this permission has been removed. -->
    <permission android:name="android.permission.READ_INPUT_STATE"
        android:label="@*android:string/permlab_readInputState"
        android:description="@*android:string/permdesc_readInputState"
        android:protectionLevel="signature" />

    <!-- Must be required by an {@link android.inputmethodservice.InputMethodService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_INPUT_METHOD"
        android:label="@*android:string/permlab_bindInputMethod"
        android:description="@*android:string/permdesc_bindInputMethod"
        android:protectionLevel="signature" />

    <!-- Must be required by an {@link android.accessibilityservice.AccessibilityService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        android:label="@*android:string/permlab_bindAccessibilityService"
        android:description="@*android:string/permdesc_bindAccessibilityService"
        android:protectionLevel="signature" />

    <!-- Must be required by a {@link android.printservice.PrintService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_PRINT_SERVICE"
        android:label="@*android:string/permlab_bindPrintService"
        android:description="@*android:string/permdesc_bindPrintService"
        android:protectionLevel="signature" />

    <!-- Must be required by a {@link android.nfc.cardemulation.HostApduService}
         or {@link android.nfc.cardemulation.OffHostApduService} to ensure that only
         the system can bind to it. -->
    <permission android:name="android.permission.BIND_NFC_SERVICE"
        android:label="@*android:string/permlab_bindNfcService"
        android:description="@*android:string/permdesc_bindNfcService"
        android:protectionLevel="signature" />

    <!-- Must be required by the PrintSpooler to ensure that only the system can bind to it.
         @hide -->
    <permission android:name="android.permission.BIND_PRINT_SPOOLER_SERVICE"
        android:label="@*android:string/permlab_bindPrintSpoolerService"
        android:description="@*android:string/permdesc_bindPrintSpoolerService"
        android:protectionLevel="signature" />

    <!-- Must be required by a TextService (e.g. SpellCheckerService)
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_TEXT_SERVICE"
        android:label="@*android:string/permlab_bindTextService"
        android:description="@*android:string/permdesc_bindTextService"
        android:protectionLevel="signature" />

    <!-- Must be required by a {@link android.net.VpnService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_VPN_SERVICE"
        android:label="@*android:string/permlab_bindVpnService"
        android:description="@*android:string/permdesc_bindVpnService"
        android:protectionLevel="signature" />

    <!-- Must be required by a {@link android.service.wallpaper.WallpaperService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_WALLPAPER"
        android:label="@*android:string/permlab_bindWallpaper"
        android:description="@*android:string/permdesc_bindWallpaper"
        android:protectionLevel="signature|system" />

    <!-- Must be required by a {@link com.android.media.remotedisplay.RemoteDisplayProvider},
         to ensure that only the system can bind to it.
         @hide -->
    <permission android:name="android.permission.BIND_REMOTE_DISPLAY"
        android:label="@*android:string/permlab_bindRemoteDisplay"
        android:description="@*android:string/permdesc_bindRemoteDisplay"
        android:protectionLevel="signature" />

    <!-- Must be required by device administration receiver, to ensure that only the
         system can interact with it. -->
    <permission android:name="android.permission.BIND_DEVICE_ADMIN"
        android:label="@*android:string/permlab_bindDeviceAdmin"
        android:description="@*android:string/permdesc_bindDeviceAdmin"
        android:protectionLevel="signature" />

    <!-- Required to add or remove another application as a device admin.
         <p>Not for use by third-party applications.
         @hide -->
    <permission android:name="android.permission.MANAGE_DEVICE_ADMINS"
        android:label="@*android:string/permlab_manageDeviceAdmins"
        android:description="@*android:string/permdesc_manageDeviceAdmins"
        android:protectionLevel="signature|system" />

    <!-- Allows low-level access to setting the orientation (actually
         rotation) of the screen.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_ORIENTATION"
        android:label="@*android:string/permlab_setOrientation"
        android:description="@*android:string/permdesc_setOrientation"
        android:protectionLevel="signature" />

    <!-- Allows low-level access to setting the pointer speed.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.SET_POINTER_SPEED"
        android:label="@*android:string/permlab_setPointerSpeed"
        android:description="@*android:string/permdesc_setPointerSpeed"
        android:protectionLevel="signature" />

    <!-- Allows low-level access to setting the keyboard layout.
         <p>Not for use by third-party applications.
         @hide -->
    <permission android:name="android.permission.SET_KEYBOARD_LAYOUT"
        android:label="@*android:string/permlab_setKeyboardLayout"
        android:description="@*android:string/permdesc_setKeyboardLayout"
        android:protectionLevel="signature" />

    <!-- Allows an application to install packages.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.INSTALL_PACKAGES"
        android:label="@*android:string/permlab_installPackages"
        android:description="@*android:string/permdesc_installPackages"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to clear user data.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.CLEAR_APP_USER_DATA"
        android:label="@*android:string/permlab_clearAppUserData"
        android:description="@*android:string/permdesc_clearAppUserData"
        android:protectionLevel="signature" />

    <!-- Allows an application to delete cache files.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.DELETE_CACHE_FILES"
        android:label="@*android:string/permlab_deleteCacheFiles"
        android:description="@*android:string/permdesc_deleteCacheFiles"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to delete packages.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.DELETE_PACKAGES"
        android:label="@*android:string/permlab_deletePackages"
        android:description="@*android:string/permdesc_deletePackages"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to move location of installed package.
         @hide -->
    <permission android:name="android.permission.MOVE_PACKAGE"
        android:label="@*android:string/permlab_movePackage"
        android:description="@*android:string/permdesc_movePackage"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to change whether an application component (other than its own) is
         enabled or not.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.CHANGE_COMPONENT_ENABLED_STATE"
        android:label="@*android:string/permlab_changeComponentState"
        android:description="@*android:string/permdesc_changeComponentState"
        android:protectionLevel="signature|system" />

    <!-- @hide Allows an application to grant or revoke specific permissions. -->
    <permission android:name="android.permission.GRANT_REVOKE_PERMISSIONS"
        android:label="@*android:string/permlab_grantRevokePermissions"
        android:description="@*android:string/permdesc_grantRevokePermissions"
        android:protectionLevel="signature" />

    <!-- Allows an application to use SurfaceFlinger's low level features.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.ACCESS_SURFACE_FLINGER"
        android:label="@*android:string/permlab_accessSurfaceFlinger"
        android:description="@*android:string/permdesc_accessSurfaceFlinger"
        android:protectionLevel="signature" />

    <!-- Allows an application to take screen shots and more generally
         get access to the frame buffer data.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.READ_FRAME_BUFFER"
        android:label="@*android:string/permlab_readFrameBuffer"
        android:description="@*android:string/permdesc_readFrameBuffer"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to configure and connect to Wifi displays
         @hide -->
    <permission android:name="android.permission.CONFIGURE_WIFI_DISPLAY"
        android:label="@*android:string/permlab_configureWifiDisplay"
        android:description="@*android:string/permdesc_configureWifiDisplay"
        android:protectionLevel="signature" />

    <!-- Allows an application to control low-level features of Wifi displays
         such as opening an RTSP socket.  This permission should only be used
         by the display manager.
         @hide -->
    <permission android:name="android.permission.CONTROL_WIFI_DISPLAY"
        android:label="@*android:string/permlab_controlWifiDisplay"
        android:description="@*android:string/permdesc_controlWifiDisplay"
        android:protectionLevel="signature" />

    <!-- Allows an application to capture audio output.
         <p>Not for use by third-party applications.</p> -->
    <permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT"
        android:label="@*android:string/permlab_captureAudioOutput"
        android:description="@*android:string/permdesc_captureAudioOutput"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to capture audio for hotword detection.
         <p>Not for use by third-party applications.</p>
         @hide -->
    <permission android:name="android.permission.CAPTURE_AUDIO_HOTWORD"
        android:label="@*android:string/permlab_captureAudioHotword"
        android:description="@*android:string/permdesc_captureAudioHotword"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to capture video output.
         <p>Not for use by third-party applications.</p> -->
    <permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT"
        android:label="@*android:string/permlab_captureVideoOutput"
        android:description="@*android:string/permdesc_captureVideoOutput"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to capture secure video output.
         <p>Not for use by third-party applications.</p> -->
    <permission android:name="android.permission.CAPTURE_SECURE_VIDEO_OUTPUT"
        android:label="@*android:string/permlab_captureSecureVideoOutput"
        android:description="@*android:string/permdesc_captureSecureVideoOutput"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to know what content is playing and control its playback.
         <p>Not for use by third-party applications due to privacy of media consumption</p>  -->
    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL"
        android:label="@*android:string/permlab_mediaContentControl"
        android:description="@*android:string/permdesc_mediaContentControl"
        android:protectionLevel="signature|system" />

    <!-- Required to be able to disable the device (very dangerous!).
    <p>Not for use by third-party applications.. -->
    <permission android:name="android.permission.BRICK"
        android:label="@*android:string/permlab_brick"
        android:description="@*android:string/permdesc_brick"
        android:protectionLevel="signature" />

    <!-- Required to be able to reboot the device.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.REBOOT"
        android:label="@*android:string/permlab_reboot"
        android:description="@*android:string/permdesc_reboot"
        android:protectionLevel="signature|system" />

   <!-- Allows low-level access to power management.
   <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.DEVICE_POWER"
        android:label="@*android:string/permlab_devicePower"
        android:description="@*android:string/permdesc_devicePower"
        android:protectionLevel="signature" />

   <!-- @hide Allows low-level access to tun tap driver -->
    <permission android:name="android.permission.NET_TUNNELING"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Run as a manufacturer test application, running as the root user.
         Only available when the device is running in manufacturer test mode.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.FACTORY_TEST"
        android:label="@*android:string/permlab_factoryTest"
        android:description="@*android:string/permdesc_factoryTest"
        android:protectionLevel="signature" />

    <!-- Allows an application to broadcast a notification that an application
         package has been removed.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.BROADCAST_PACKAGE_REMOVED"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permlab_broadcastPackageRemoved"
        android:description="@*android:string/permdesc_broadcastPackageRemoved"
        android:protectionLevel="signature" />

    <!-- Allows an application to broadcast an SMS receipt notification.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.BROADCAST_SMS"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:label="@*android:string/permlab_broadcastSmsReceived"
        android:description="@*android:string/permdesc_broadcastSmsReceived"
        android:protectionLevel="signature" />

    <!-- Allows an application to broadcast a WAP PUSH receipt notification.
    <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.BROADCAST_WAP_PUSH"
        android:permissionGroup="android.permission-group.MESSAGES"
        android:label="@*android:string/permlab_broadcastWapPush"
        android:description="@*android:string/permdesc_broadcastWapPush"
        android:protectionLevel="signature" />

    <!-- Not for use by third-party applications. -->
    <permission android:name="android.permission.MASTER_CLEAR"
        android:label="@*android:string/permlab_masterClear"
        android:description="@*android:string/permdesc_masterClear"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to call any phone number, including emergency
         numbers, without going through the Dialer user interface for the user
         to confirm the call being placed.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.CALL_PRIVILEGED"
        android:label="@*android:string/permlab_callPrivileged"
        android:description="@*android:string/permdesc_callPrivileged"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to perform CDMA OTA provisioning @hide -->
    <permission android:name="android.permission.PERFORM_CDMA_PROVISIONING"
        android:label="@*android:string/permlab_performCdmaProvisioning"
        android:description="@*android:string/permdesc_performCdmaProvisioning"
        android:protectionLevel="signature|system" />

    <!-- Allows enabling/disabling location update notifications from
         the radio.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.CONTROL_LOCATION_UPDATES"
        android:label="@*android:string/permlab_locationUpdates"
        android:description="@*android:string/permdesc_locationUpdates"
        android:protectionLevel="signature|system" />

    <!-- Allows read/write access to the "properties" table in the checkin
         database, to change values that get uploaded.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.ACCESS_CHECKIN_PROPERTIES"
        android:label="@*android:string/permlab_checkinProperties"
        android:description="@*android:string/permdesc_checkinProperties"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to collect component usage
         statistics @hide -->
    <permission android:name="android.permission.PACKAGE_USAGE_STATS"
        android:label="@*android:string/permlab_pkgUsageStats"
        android:description="@*android:string/permdesc_pkgUsageStats"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to collect battery statistics -->
    <permission android:name="android.permission.BATTERY_STATS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:label="@*android:string/permlab_batteryStats"
        android:description="@*android:string/permdesc_batteryStats"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to control the backup and restore process.
    <p>Not for use by third-party applications.
         @hide pending API council -->
    <permission android:name="android.permission.BACKUP"
        android:label="@*android:string/permlab_backup"
        android:description="@*android:string/permdesc_backup"
        android:protectionLevel="signature|system" />

    <!-- Allows a package to launch the secure full-backup confirmation UI.
         ONLY the system process may hold this permission.
         @hide -->
    <permission android:name="android.permission.CONFIRM_FULL_BACKUP"
        android:label="@*android:string/permlab_confirm_full_backup"
        android:description="@*android:string/permdesc_confirm_full_backup"
        android:protectionLevel="signature" />

    <!-- Must be required by a {@link android.widget.RemoteViewsService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_REMOTEVIEWS"
        android:label="@*android:string/permlab_bindRemoteViews"
        android:description="@*android:string/permdesc_bindRemoteViews"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to tell the AppWidget service which application
         can access AppWidget's data.  The normal user flow is that a user
         picks an AppWidget to go into a particular host, thereby giving that
         host application access to the private data from the AppWidget app.
         An application that has this permission should honor that contract.
         <p>Not for use by third-party applications. -->
    <permission android:name="android.permission.BIND_APPWIDGET"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:label="@*android:string/permlab_bindGadget"
        android:description="@*android:string/permdesc_bindGadget"
        android:protectionLevel="signature|system" />

    <!-- Private permission, to restrict who can bring up a dialog to add a new
         keyguard widget
         @hide -->
    <permission android:name="android.permission.BIND_KEYGUARD_APPWIDGET"
        android:permissionGroup="android.permission-group.PERSONAL_INFO"
        android:protectionLevel="signature|system" />

    <!-- Internal permission allowing an application to query/set which
         applications can bind AppWidgets.
         @hide -->
    <permission android:name="android.permission.MODIFY_APPWIDGET_BIND_PERMISSIONS"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system" />

    <!-- Allows applications to change the background data setting.
    <p>Not for use by third-party applications.
         @hide pending API council -->
    <permission android:name="android.permission.CHANGE_BACKGROUND_DATA_SETTING"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature"
        android:description="@*android:string/permdesc_changeBackgroundDataSetting"
        android:label="@*android:string/permlab_changeBackgroundDataSetting" />

    <!-- This permission can be used on content providers to allow the global
         search system to access their data.  Typically it used when the
         provider has some permissions protecting it (which global search
         would not be expected to hold), and added as a read-only permission
         to the path in the provider where global search queries are
         performed.  This permission can not be held by regular applications;
         it is used by applications to protect themselves from everyone else
         besides global search. -->
    <permission android:name="android.permission.GLOBAL_SEARCH"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system" />

    <!-- Internal permission protecting access to the global search
         system: ensures that only the system can access the provider
         to perform queries (since this otherwise provides unrestricted
         access to a variety of content providers), and to write the
         search statistics (to keep applications from gaming the source
         ranking).
         @hide -->
    <permission android:name="android.permission.GLOBAL_SEARCH_CONTROL"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Allows applications to set a live wallpaper.
         @hide XXX Change to signature once the picker is moved to its
         own apk as Ghod Intended. -->
    <permission android:name="android.permission.SET_WALLPAPER_COMPONENT"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature|system" />

    <!-- Allows applications to read dream settings and dream state.
         @hide -->
    <permission android:name="android.permission.READ_DREAM_STATE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Allows applications to write dream settings, and start or stop dreaming.
         @hide -->
    <permission android:name="android.permission.WRITE_DREAM_STATE"
        android:permissionGroup="android.permission-group.SYSTEM_TOOLS"
        android:protectionLevel="signature" />

    <!-- Allow an application to read and write the cache partition.
         @hide -->
    <permission android:name="android.permission.ACCESS_CACHE_FILESYSTEM"
        android:label="@*android:string/permlab_cache_filesystem"
        android:description="@*android:string/permdesc_cache_filesystem"
        android:protectionLevel="signature|system" />

    <!-- Must be required by default container service so that only
         the system can bind to it and use it to copy
         protected data to secure containers or files
         accessible to the system.
         @hide -->
    <permission android:name="android.permission.COPY_PROTECTED_DATA"
        android:label="@*android:string/permlab_copyProtectedData"
        android:description="@*android:string/permlab_copyProtectedData"
        android:protectionLevel="signature" />

    <!-- Internal permission protecting access to the encryption methods
        @hide
    -->
    <permission android:name="android.permission.CRYPT_KEEPER"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to read historical network usage for
         specific networks and applications. @hide -->
    <permission android:name="android.permission.READ_NETWORK_USAGE_HISTORY"
        android:label="@*android:string/permlab_readNetworkUsageHistory"
        android:description="@*android:string/permdesc_readNetworkUsageHistory"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to manage network policies (such as warning and disable
         limits) and to define application-specific rules. @hide -->
    <permission android:name="android.permission.MANAGE_NETWORK_POLICY"
        android:label="@*android:string/permlab_manageNetworkPolicy"
        android:description="@*android:string/permdesc_manageNetworkPolicy"
        android:protectionLevel="signature" />

    <!-- Allows an application to account its network traffic against other UIDs. Used
         by system services like download manager and media server. Not for use by
         third party apps. @hide -->
    <permission android:name="android.permission.MODIFY_NETWORK_ACCOUNTING"
        android:label="@*android:string/permlab_modifyNetworkAccounting"
        android:description="@*android:string/permdesc_modifyNetworkAccounting"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to mark traffic as from another user for per user routing.
         Used by system wide services like media server that execute delegated network connections
         for users.
         @hide
    -->
    <permission android:name="android.permission.MARK_NETWORK_SOCKET"
        android:label="@*android:string/permlab_markNetworkSocket"
        android:description="@*android:string/permdesc_markNetworkSocket"
        android:protectionLevel="signature|system" />

    <!-- C2DM permission.
         @hide Used internally.
     -->
    <permission android:name="android.intent.category.MASTER_CLEAR.permission.C2D_MESSAGE"
          android:protectionLevel="signature" />
    <uses-permission android:name="android.intent.category.MASTER_CLEAR.permission.C2D_MESSAGE"/>

    <!-- @hide Package verifier needs to have this permission before the PackageManager will
         trust it to verify packages.
    -->
    <permission android:name="android.permission.PACKAGE_VERIFICATION_AGENT"
        android:label="@*android:string/permlab_packageVerificationAgent"
        android:description="@*android:string/permdesc_packageVerificationAgent"
        android:protectionLevel="signature|system" />

    <!-- Must be required by package verifier receiver, to ensure that only the
         system can interact with it.
         @hide
    -->
    <permission android:name="android.permission.BIND_PACKAGE_VERIFIER"
        android:label="@*android:string/permlab_bindPackageVerifier"
        android:description="@*android:string/permdesc_bindPackageVerifier"
        android:protectionLevel="signature" />

    <!-- Allows applications to access serial ports via the SerialManager.
         @hide -->
    <permission android:name="android.permission.SERIAL_PORT"
        android:label="@*android:string/permlab_serialPort"
        android:description="@*android:string/permdesc_serialPort"
        android:protectionLevel="signature|system" />

    <!-- Allows the holder to access content providers from outside an ApplicationThread.
         This permission is enforced by the ActivityManagerService on the corresponding APIs,
         in particular ActivityManagerService#getContentProviderExternal(String) and
         ActivityManagerService#removeContentProviderExternal(String).
         @hide
    -->
    <permission android:name="android.permission.ACCESS_CONTENT_PROVIDERS_EXTERNALLY"
        android:label="@*android:string/permlab_accessContentProvidersExternally"
        android:description="@*android:string/permdesc_accessContentProvidersExternally"
        android:protectionLevel="signature" />
    <!-- Allows an application to hold an UpdateLock, recommending that a headless
         OTA reboot *not* occur while the lock is held.
         @hide -->
    <permission android:name="android.permission.UPDATE_LOCK"
        android:label="@*android:string/permlab_updateLock"
        android:description="@*android:string/permdesc_updateLock"
        android:protectionLevel="signatureOrSystem" />

    <!-- Allows an application to read the current set of notifications, including
         any metadata and intents attached.
         @hide -->
    <permission android:name="android.permission.ACCESS_NOTIFICATIONS"
        android:label="@*android:string/permlab_accessNotifications"
        android:description="@*android:string/permdesc_accessNotifications"
        android:protectionLevel="signature|system" />

    <!-- Allows access to keyguard secure storage.  Only allowed for system processes.
        @hide -->
    <permission android:name="android.permission.ACCESS_KEYGUARD_SECURE_STORAGE"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_access_keyguard_secure_storage"
        android:description="@*android:string/permdesc_access_keyguard_secure_storage" />

    <!-- Allows an application to control keyguard.  Only allowed for system processes.
        @hide -->
    <permission android:name="android.permission.CONTROL_KEYGUARD"
        android:protectionLevel="signature"
        android:label="@*android:string/permlab_control_keyguard"
        android:description="@*android:string/permdesc_control_keyguard" />

    <!-- Must be required by an {@link
         android.service.notification.NotificationListenerService},
         to ensure that only the system can bind to it. -->
    <permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        android:label="@*android:string/permlab_bindNotificationListenerService"
        android:description="@*android:string/permdesc_bindNotificationListenerService"
        android:protectionLevel="signature" />

    <!-- Allows an application to call into a carrier setup flow. It is up to the
         carrier setup application to enforce that this permission is required
         @hide This is not a third-party API (intended for OEMs and system apps). -->
    <permission android:name="android.permission.INVOKE_CARRIER_SETUP"
        android:label="@*android:string/permlab_invokeCarrierSetup"
        android:description="@*android:string/permdesc_invokeCarrierSetup"
        android:protectionLevel="signature|system" />

    <!-- Allows an application to listen for network condition observations.
         @hide This is not a third-party API (intended for system apps). -->
    <permission android:name="android.permission.ACCESS_NETWORK_CONDITIONS"
        android:label="@*android:string/permlab_accessNetworkConditions"
        android:description="@*android:string/permdesc_accessNetworkConditions"
        android:protectionLevel="signature|system" />

    <!-- The system process is explicitly the only one allowed to launch the
         confirmation UI for full backup/restore -->
    <uses-permission android:name="android.permission.CONFIRM_FULL_BACKUP"/>


    <application
        android:allowBackup="true"
        android:label="@null">
        
        
        
        <activity
            android:name="com.tcl.tvos_res.MainActivity"
            android:label="@null" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
